#include <stdio.h>

void main(){
    int pulo[3] = {10, 100, 1000};

    printf("%d\n", *(pulo + 2));
    printf("%d\n", *(pulo + 4));
    printf("%d\n", pulo + 4);
    printf("%d\n", pulo + 2);

    // Apenas a primeira expressão (*(pulo+2)) referencia o valor do terceiro elemento do vetor.
}
