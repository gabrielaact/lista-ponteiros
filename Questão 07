#include <stdio.h>

void main(){
    int mat[4], *p, x;
    
    p = mat + 1;
    // p = mat++; - Expressão inválida, pois esse tipo de incremento não funciona para vetores.
    // p = ++mat; - Expressão inválida, pois esse tipo de incremento não funciona para vetores.
    x = (*mat)++;
}
